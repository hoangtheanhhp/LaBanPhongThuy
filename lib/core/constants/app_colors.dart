import 'package:flutter/material.dart';

class AppColors {
  // OLED & Neutral Backgrounds
  static const Color darkBackground = Color(0xFF121212);
  static const Color surfaceCard = Color(0xFF1E1E1E);
  static const Color surfaceElevated = Color(0xFF2A2A2A);

  // Accents
  static const Color primaryGold = Color(0xFFFFD700);
  static const Color accentAmber = Color(0xFFFFB300);
  static const Color northRed = Color(0xFFFF3B30);

  // Bát Trạch Auspicious / Inauspicious
  static const Color goodDirection = Color(0xFF34C759); // Green (Sinh Khí, Diên Niên, etc.)
  static const Color badDirection = Color(0xFFFF453A);  // Red (Tuyệt Mệnh, Ngũ Quỷ, etc.)
}
