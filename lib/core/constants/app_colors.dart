import 'package:flutter/material.dart';

class AppColors {
  // Minimalist Wood & Monochrome Palette
  static const Color darkBackground = Color(0xFF141312); // Nền gỗ mun tối
  static const Color surfaceCard = Color(0xFF1E1C1A);     // Thẻ màu gỗ óc chó ấm
  static const Color surfaceElevated = Color(0xFF282522); // Gỗ đàn hương nổi

  // Tông gỗ & điểm nhấn
  static const Color woodAccent = Color(0xFFC49A6C);     // Màu gỗ tếch sáng / vàng đồng cổ
  static const Color woodBorder = Color(0xFF5A4D41);     // Đường chỉ gỗ
  static const Color ivoryWhite = Color(0xFFFAF6EE);     // Trắng ngà giấy dó
  static const Color charcoalBlack = Color(0xFF141414);  // Mực đen tuyền
  static const Color northRed = Color(0xFF9E2A2B);       // Đỏ chu sa truyền thống

  // Bát Trạch Auspicious / Inauspicious (Mộc mạc, sang trọng)
  static const Color goodDirection = Color(0xFF8D6E53);  // Nâu gỗ sáng cho Cát
  static const Color badDirection = Color(0xFF3E3835);   // Nâu gỗ cháy cho Hung
}
